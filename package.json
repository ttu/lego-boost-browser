{
  "name": "lego-boost-browser",
  "version": "0.1.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "tsc",
    "build:browser": "npx browserify ./src/browser.ts --debug -p [ tsify ] > ./dist/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^16.2.3",
    "tsify": "^4.0.1",
    "typescript": "^3.2.1"
  },
  "dependencies": {
    "@types/web-bluetooth": "0.0.4"
  }
}
